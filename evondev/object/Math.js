//Math object

/**
 * math.Pi
 * math.round()
 * math.abs() ->trả về giá trị tuyệt đối
 * math.ceil() -> làm tròn trên
 * math.floor() -> làm tròn dưới
 * math.random() -> trả vè dãy số thập phân nhỏ hơn 1
 * math.min()
 * math.max()
 */

const random = Math.floor(Math.random() * 100);

if (random < 5) {
  console.log("cường hóa thành công");
}
